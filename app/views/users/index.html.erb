<table>
  <thead>
    <tr>
      <th>image</th>
      <th>name</th>
      <th colspan ="3"></th>
    </tr>
  </thead>
  <tboady>
    <% @users.each do |user| %>
    <tr>
      <td><img src=<%= user.image %> class = "icon_image"></td>
      <td><%= user.name %></td>
      <td><%= link_to "Show", user_path(user) %></td>
    </tr>
    <% end %>
  </tboady>
</table>