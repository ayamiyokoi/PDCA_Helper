<div class="container">
  <div class="row">
    <div class="groups col-md-12">
      <% current_user.groups.each do |group| %>
        <div class="group">
          <%= link_to '#' do %>
            <div class="group__name">
              <%= group.name %>
            </div>
            <div class="member">
              メンバー:
              <% group.users.each do |user| %>
              <%= attachment_image_tag user, :profile_image, size: "50x50", format: 'jpeg', fallback: "no_image.jpg" %><%= user.name %>
              <% end %>
            </div>
            <div class="group__message">
              メッセージはまだありません。
            </div>
          <% end %>
          <%= link_to '編集', edit_group_path(group), class: "btn btn-outline-success" %>  <%= link_to '削除', group, class: "btn btn-outline-danger", method: :delete, data: { confirm: 'Are you sure?' } %>
        </div>
      <% end %>
    </div>

  </div>
<div class="row">
  <div class="col-md-12">
  <div class="chat-group-form">
  <h3>新規チームを作成  <i class="far fa-comments"></i></h3>
  <%= render :partial => 'form', :locals => {group: @group} %>
  </div>
  </div>
</div>
</div>